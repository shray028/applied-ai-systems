# Autonomous Drone Rescue Planner

Value iteration finds a shortest safe route through a configurable grid world. The visual output marks the start, goal, obstacles, and selected policy path.

```bash
python drone_rescue/planner.py --render
```
